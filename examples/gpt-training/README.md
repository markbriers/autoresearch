# GPT Training Example

This is the original autoresearch experiment: optimising a small GPT model's val_bpb through cross-domain paper synthesis.

To run this example:

```bash
cp examples/gpt-training/task.md ./task.md
# Then follow the standard setup and run instructions in the root README
```

For the full write-up covering nine protocol iterations and 200+ experiments, see [docs/blog-post.md](../../docs/blog-post.md).

Results from the original experiments are in `results/v1` through `results/v9`.
